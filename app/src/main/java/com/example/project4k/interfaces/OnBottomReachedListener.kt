package com.example.project4k.interfaces

// this interface is used to let us know that we are at the bottom of the recycler view
interface OnBottomReachedListener {
    fun onBottomReached(position: Int)
}
